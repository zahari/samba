dependencies:
    pre:
        - sudo apt-get install autoconf-archive automake-1.14 autoconf2.59 build-essential
test:
    pre:
        - ./configure
        - make
    override:
        - make test
